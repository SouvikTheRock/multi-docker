name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t souviktherock/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true souviktherock/react-test npm test

      - run: docker build -t souviktherock/multi-client ./client
      - run: docker build -t souviktherock/multi-nginx ./nginx
      - run: docker build -t souviktherock/multi-server ./server
      - run: docker build -t souviktherock/multi-worker ./worker

      - run: docker push souviktherock/multi-client
      - run: docker push souviktherock/multi-nginx
      - run: docker push souviktherock/multi-server
      - run: docker push souviktherock/multi-worker
